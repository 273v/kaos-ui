# Streamlit's native secrets file.
#
# This template uses .env via pydantic-settings instead — see .env.example.
# Streamlit's st.secrets is available as a fallback if you want to use it,
# but you don't need to touch this file for the default flow.
#
# If you do use it, copy this to secrets.toml (which is gitignored).
