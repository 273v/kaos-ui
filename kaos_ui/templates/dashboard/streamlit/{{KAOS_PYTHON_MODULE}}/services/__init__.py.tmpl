"""Business-logic services for {{KAOS_PROJECT_NAME}}.

Pages stay thin — they bind UI controls to functions in this package.
Each service module is self-contained; cross-service calls go through
the runtime and VFS, never through service-to-service imports.
"""
