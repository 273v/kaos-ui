"""Business-logic services for {{KAOS_PROJECT_NAME}}.

Heavy KAOS imports (kaos-agents, kaos-content) live inside function
bodies so screen modules can import services cheaply. The TUI stays
bootable even when an extra package is missing.
"""
